Get-ChildItem -Recurse -Directory -Filter __pycache__ | Remove-Item -Recurse -Force
python -m techdeck


thickness
material
quantity in part name

# Replace YOUR_USERNAME with your GitHub username
git remote add origin https://github.com/OzymandiasOne/TechDeck.git




TO UPDATE CURRENT BUILD

# 1. Edit techdeck\core\constants.py:
#    APP_VERSION = "0.7.3"
#    APP_RELEASE_NAME = "Bug Fixes and Improvements"

# 2. Stage and commit
git add .
git status
git commit -m "Release v0.7.3 - Bug fixes and improvements"

# 3. Tag and push
git tag v0.7.3
git push origin main
git push origin v0.7.3



TO CHECK CURRENT VERSION AND GIT STATUS

# See current version in constants.py
Get-Content techdeck\core\constants.py | Select-String "APP_VERSION"

# See all local tags
git tag

# See if you're currently on a tag
git describe --tags

# See latest commit
git log --oneline -1

# See current branch
git branch

# See if the tag v0.7.1 exists locally
git tag -l "v0.7.1"

# See if the tag exists on remote (GitHub)
git ls-remote --tags origin