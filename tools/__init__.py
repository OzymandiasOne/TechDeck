"""Developer/authoring tools for building TechDeck itself.

This package is EXCLUDED from the frozen build (see TechDeck.spec `excludes`)
and is only importable when running from source. Nothing here ships to users.
"""
