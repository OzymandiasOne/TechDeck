# TechDeck pinned dependencies
# -----------------------------
# Reproducible install for building the frozen exe (see the techdeck-release
# skill) and for running `python -m techdeck` in dev. Pins are the versions the
# current shipping build was produced with — bump deliberately, then rebuild +
# test, never float.
#
#   python -m pip install -r requirements.txt
#
# Anything a plugin imports dynamically must ALSO be listed in TechDeck.spec
# hiddenimports (see Hard Rule 8) — this file governs what's installed, the spec
# governs what PyInstaller bundles.

# --- GUI toolkit ---
PySide6==6.9.1            # pulls PySide6-Essentials/Addons + shiboken6 (incl. QtCharts)

# --- Networking / updates / telemetry ---
requests==2.32.4
packaging==25.0           # version comparison in update_checker

# --- Excel / data ---
openpyxl==3.1.5
pandas==2.3.3
numpy==2.2.2              # pandas dependency; pinned for reproducibility

# --- PDF ---
PyMuPDF==1.26.3           # imported as `fitz`
pypdf==6.4.1

# --- Misc plugin deps ---
qrcode==8.2               # QR Code Generator plugin
Pillow==12.2.0            # imported as `PIL`; QR image render + asset tooling

# --- Drawing reader (911 Inspection Dimensions) ---
# On-device OCR: no network call, no external exe, models ship inside the package.
# rapidocr-onnxruntime pulls onnxruntime + opencv-python + pyclipper + Shapely + PyYAML.
rapidocr-onnxruntime==1.2.3
onnxruntime==1.27.0
opencv-python==4.13.0.92   # imported as `cv2`; a rapidocr dependency

# --- Build only (not imported at runtime) ---
pyinstaller==6.15.0
